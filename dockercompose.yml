version: '3'
services:
  frontend:
    build: ./frontend
    container_name: frontend-container
    ports:
      - 6400:6400
    restart: always
  
  backend:
    build: ./backend
    container_name: backend-container 
    ports:
     - 7400:7400
    restart: always 

    depends_on:
      - backend